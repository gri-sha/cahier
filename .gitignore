# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local embedded postgres
/.pglite/

# playwright mcp artifacts
/.playwright-mcp/
!.env.example

# private knowledge base — agent docs + skills (never committed; only code +
# README are public). DEPLOY.md's content now lives in .claude/skills/cahier-ops.
/CLAUDE.md
/.claude/
DEPLOY.md

# email preview (bun run email:test)
/email-preview.html
